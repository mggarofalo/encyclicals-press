# Copyright

The code, templates, and tooling in this repository are released under the [MIT License](LICENSE). Use them freely, including commercially.

When you run `encyclicals fetch`, real vatican.va HTML lands in the gitignored `input/` directory, and `ingest` writes the parsed markdown to the gitignored `corpus/` directory. Those translations are © Libreria Editrice Vaticana (LEV) and never enter the repository — every committed HTML file under `tests/fixtures/` is lorem-ipsum'd. This project reproduces the translations locally for personal study and scholarly use, is not commercial, and does not redistribute the texts for profit. If LEV or the USCCB asks that any document be removed, it will be removed without dispute. No sweeping fair-use claim is made.
